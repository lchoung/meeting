package com.requiris.meeting.Mongo;

import android.os.AsyncTask;

public class GetCollNumTask  extends AsyncTask<String, Void, Long> {
	protected Long doInBackground(String... urls) {
		try {


		} catch (Exception e) {
			return (long)-1;
		}

		return (long)1;
	}
}
